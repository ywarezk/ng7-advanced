import { IsIdCorrectDirective } from './is-id-correct.directive';

describe('IsIdCorrectDirective', () => {
  it('should create an instance', () => {
    const directive = new IsIdCorrectDirective();
    expect(directive).toBeTruthy();
  });
});
