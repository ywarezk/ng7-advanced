import { AsyncValidDirective } from './async-valid.directive';

describe('AsyncValidDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncValidDirective();
    expect(directive).toBeTruthy();
  });
});
